# config.py

# VULN_MODE toggles the vulnerability state for each module.
# True = Vulnerable (educational/demonstration mode)
# False = Patched (secure mode)
VULN_MODE = {
    "sqli": True,
    "xss": True,
    "session": True,
}
